server:
  port: 8080

database:
  host: localhost
  port: 3306
  user: root
  password: "your_password"
  dbname: mem_test
  charset: utf8mb4

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

dify:
  # Dify API 配置
  base_url: http://dify.ickey.com.cn/v1
  api_key: app-4kBYdLfSFgL2HZ3NTeUYpdpL
  
  # 如果启用 mock 模式，则不调用真实 Dify API（用于测试）
  mock_mode: false
  
  # 指定应用类型（可选值：agent, chat, completion, workflow）
  app_type: "agent"
  
  # 自定义端点（如果默认端点不工作）
  # custom_endpoint: "chat-messages"  # 例如：chat-messages, completion-messages, workflows/run

